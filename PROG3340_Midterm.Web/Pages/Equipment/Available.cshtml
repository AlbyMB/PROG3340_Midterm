@page
@model PROG3340_Midterm.Web.Pages.Equipment.AvailableModel
<h2>Available Equipment</h2>

@if (!string.IsNullOrEmpty(Model.Error))
{
    <div class="alert alert-danger">@Model.Error</div>
}
@if (!string.IsNullOrEmpty(Model.Success))
{
    <div class="alert alert-success">@Model.Success</div>
}

<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Price</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var e in Model.Items)
    {
        <tr>
            <td>@e.Name</td>
            <td>@e.RentalPrice</td>
            <td>
                <a asp-page="/Equipment/Details" asp-route-id="@e.Id" class="btn btn-sm btn-outline-secondary">Details</a>
                <a asp-page="/Rentals/Request" asp-route-equipmentId="@e.Id" class="btn btn-sm btn-primary ms-1">Request</a>
            </td>
        </tr>
    }
    </tbody>
 </table>
